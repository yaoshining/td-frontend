define(['angular','users/module'],function(angular,usersModule){
    usersModule.controller('LoginController', [
        '$scope',
        '$http',
        function(
            $scope,
            $http
            ){

        }
    ]);
});